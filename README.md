<div dir="rtl">

# 🌊 محيط (Ocean) - الصدفة العربية

<div align="center">

[![Rust](https://img.shields.io/badge/Rust-1.70+-orange.svg)](https://www.rust-lang.org/)
[![License](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Tarqeem](https://img.shields.io/badge/Tarqeem-Project-green.svg)](https://github.com/osama1998H/tarqeem)
[![Tests](https://img.shields.io/badge/Tests-29%20passing-brightgreen.svg)]()

**صدفة حديثة وخفيفة بأوامر عربية بالكامل**

*A modern, lightweight shell with Arabic commands*

</div>

---

## 🎯 الرؤية

محيط هي صدفة (shell) ثورية مصممة للناطقين بالعربية. جميع الأوامر باللغة العربية، مما يتيح تجربة حوسبة طبيعية لملايين المستخدمين العرب.

> "حقبة جديدة للتحرر من هيمنة اللغة الإنجليزية"

---

## ✨ المميزات

- 🔤 **أوامر عربية أصلية** - `اطبع`، `اعرض`، `انتقل`، `ابحث`، إلخ
- 📝 **دعم RTL** - مصممة من البداية للنصوص من اليمين لليسار
- 🔗 **الأنابيب والتوجيه** - `|`، `>`، `>>`، `<` بدعم كامل
- ⚡ **تسلسل الأوامر** - `&&`، `||`، `;` للتحكم في التنفيذ
- 🔍 **بحث متقدم** - أمر `ابحث` للبحث في الملفات
- 🚀 **حديثة وسريعة** - مكتوبة بلغة Rust
- 💻 **متعددة المنصات** - تعمل على Linux و macOS و Windows

---

## 📦 التثبيت

### من المصدر

```bash
# استنساخ المستودع
git clone https://github.com/osama1998H/ocean.git
cd ocean

# البناء
cargo build --release

# التثبيت
cargo install --path .
```

### المتطلبات

- [Rust](https://rustup.rs/) 1.70 أو أحدث

---

## 🚀 البدء السريع

```bash
# تشغيل محيط
ocean

# أو من مجلد المشروع
cargo run
```

سترى:
```
╔═══════════════════════════════════════════════════════════╗
║   🌊  محيط (Ocean) - الصدفة العربية                        ║
║       Arabic Shell v0.1.0                                  ║
║   ✨ دعم الأنابيب والتوجيه: cmd1 | cmd2, cmd > file        ║
╚═══════════════════════════════════════════════════════════╝

محيط [~/]>
```

---

## 📖 الأوامر

### الأوامر الأساسية

| الأمر | المعادل | الوصف |
|-------|---------|-------|
| `مساعدة` | help | عرض المساعدة |
| `خروج` | exit | الخروج من الصدفة |
| `امسح` | clear | مسح الشاشة |
| `اصدار` | version | عرض الإصدار |

### أوامر الملفات

| الأمر | المعادل | الوصف |
|-------|---------|-------|
| `اطبع <نص>` | echo | طباعة نص |
| `اين` | pwd | عرض المسار الحالي |
| `انتقل <مسار>` | cd | الانتقال إلى مجلد |
| `اعرض [مسار]` | ls | عرض محتويات المجلد |
| `اقرأ <ملف>` | cat | قراءة محتوى ملف |
| `انشئ <مجلد>` | mkdir | إنشاء مجلد |
| `المس <ملف>` | touch | إنشاء ملف فارغ |
| `احذف <ملف>` | rm | حذف ملف أو مجلد |
| `انسخ <من> <إلى>` | cp | نسخ ملف |
| `انقل <من> <إلى>` | mv | نقل أو إعادة تسمية ملف |
| `ابحث <نمط> [ملف]` | grep | البحث في النص |
| `صلاحيات <وضع> <ملف>` | chmod | تغيير صلاحيات الملف |

### العوامل (Operators)

| العامل | الاسم | الوصف |
|--------|-------|-------|
| `\|` | أنبوب | توصيل مخرج الأمر الأول بمدخل الثاني |
| `>` | إلى | كتابة المخرج إلى ملف |
| `>>` | الحق | إضافة المخرج إلى نهاية ملف |
| `<` | من | قراءة المدخل من ملف |
| `&&` | و | تنفيذ الثاني إذا نجح الأول |
| `\|\|` | أو | تنفيذ الثاني إذا فشل الأول |
| `;` | ثم | تنفيذ الأوامر بالترتيب |

### أمثلة

```bash
# طباعة رسالة
محيط> اطبع مرحباً بالعالم!
مرحباً بالعالم!

# عرض المسار الحالي
محيط> اين
/Users/ahmed/projects

# الانتقال إلى مجلد
محيط> انتقل ~/المستندات

# عرض الملفات
محيط> اعرض
ملفاتي/
مشاريع/
صور/
README.md

# استخدام الأنابيب
محيط> اقرأ ملف.txt | ابحث كلمة
سطر يحتوي على كلمة

# إعادة التوجيه إلى ملف
محيط> اطبع "مرحباً" > output.txt

# تسلسل الأوامر
محيط> انشئ مجلد && انتقل مجلد

# البحث في ملف
محيط> ابحث "نمط" ملف.txt
3:سطر يحتوي على نمط

# تغيير الصلاحيات
محيط> صلاحيات 755 script.sh
```

---

## 🏗️ البنية المعمارية

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Lexer     │ ──▶ │   Parser    │ ──▶ │  Executor   │
│ (التحليل)   │     │ (البناء)    │     │ (التنفيذ)   │
└─────────────┘     └─────────────┘     └─────────────┘
      │                   │                   │
      ▼                   ▼                   ▼
   Tokens              AST              CommandResult
```

- **Lexer**: تحليل النص إلى رموز (tokens) مع دعم كامل للعربية
- **Parser**: بناء شجرة الأوامر (AST) مع دعم الأنابيب والتوجيه
- **Executor**: تنفيذ الأوامر مع تمرير البيانات بين الأنابيب

---

## 🗺️ خارطة الطريق

### المرحلة 1: الأساس ✅
- [x] حلقة REPL الأساسية
- [x] الأوامر الأساسية (14 أمر)
- [x] دعم Unicode/العربية

### المرحلة 2: الأنابيب والتوجيه ✅
- [x] الأنابيب (`|`)
- [x] إعادة التوجيه (`>`, `<`, `>>`)
- [x] تسلسل الأوامر (`&&`, `||`, `;`)
- [x] `ابحث` (grep) - البحث في الملفات
- [x] `صلاحيات` (chmod) - تغيير الصلاحيات
- [x] 29 اختبار وحدة

### المرحلة 3: ميزات متقدمة
- [ ] `مالك` (chown) - تغيير المالك
- [ ] `رابط` (ln) - إنشاء روابط
- [ ] الإكمال التلقائي
- [ ] تلوين الأوامر
- [ ] سجل الأوامر (history)

### المرحلة 4: تكامل ترقيم
- [ ] تشغيل ملفات `.trq` مباشرة
- [ ] وضع REPL لترقيم
- [ ] رسائل خطأ عربية

### المرحلة 5: التلميع
- [ ] ملف تكوين (`~/.محيط`)
- [ ] الأسماء البديلة (aliases)
- [ ] الدوال
- [ ] السمات (themes)

---

## 🧪 الاختبارات

```bash
# تشغيل جميع الاختبارات
cargo test

# تشغيل مع التفاصيل
cargo test -- --nocapture
```

**الاختبارات الحالية:**
- اختبارات Lexer: 13
- اختبارات Parser: 13
- اختبارات AST: 3
- **المجموع: 29 اختبار ناجح**

---

## 🤝 المساهمة

نرحب بمساهماتكم! يرجى قراءة [دليل المساهمة](CONTRIBUTING.md).

```bash
# استنساخ المستودع
git clone https://github.com/osama1998H/ocean.git

# إنشاء فرع جديد
git checkout -b feature/ميزة-جديدة

# تشغيل الاختبارات
cargo test

# إرسال التغييرات
git push origin feature/ميزة-جديدة
```

---

## 📄 الرخصة

محيط مرخصة تحت [رخصة MIT](LICENSE).

---

## 🙏 شكر وتقدير

- [مشروع ترقيم](https://github.com/osama1998H/tarqeem) - لغة البرمجة العربية
- [Nushell](https://www.nushell.sh/) - إلهام التصميم الحديث
- مجتمع Rust العربي

---

<div align="center">

**صُنع بـ ❤️ في مشروع ترقيم**

[الموقع](https://tarqeem.dev) • [تويتر](https://twitter.com/tarqeem) • [Discord](https://discord.gg/tarqeem)

</div>

</div>

---

# 🌊 Ocean - Arabic Shell (English)

A modern, lightweight shell with Arabic commands. Part of the Tarqeem Arabic Programming Ecosystem.

## Features

- **Native Arabic Commands**: All commands in Arabic (`اطبع`, `ابحث`, `انتقل`, etc.)
- **Pipes & Redirection**: Full support for `|`, `>`, `>>`, `<`
- **Command Chaining**: `&&`, `||`, `;` operators
- **Search**: Built-in `ابحث` (grep) command
- **29 Unit Tests**: Comprehensive test coverage

## Quick Start

```bash
# Clone and build
git clone https://github.com/osama1998H/ocean.git
cd ocean
cargo build --release

# Run
./target/release/ocean
```

## Commands

| Arabic | English | Description |
|--------|---------|-------------|
| `اطبع` | echo | Print text |
| `اعرض` | ls | List files |
| `انتقل` | cd | Change directory |
| `اين` | pwd | Current directory |
| `اقرأ` | cat | Read file |
| `انشئ` | mkdir | Create directory |
| `احذف` | rm | Delete file |
| `ابحث` | grep | Search in text |
| `صلاحيات` | chmod | Change permissions |
| `خروج` | exit | Exit shell |

## Operators

| Operator | Description |
|----------|-------------|
| `\|` | Pipe output to next command |
| `>` | Redirect output to file |
| `>>` | Append output to file |
| `<` | Read input from file |
| `&&` | Run next if previous succeeds |
| `\|\|` | Run next if previous fails |
| `;` | Run commands in sequence |

## Examples

```bash
# Pipe commands
محيط> اقرأ file.txt | ابحث pattern

# Redirect to file
محيط> اطبع "Hello" > output.txt

# Chain commands
محيط> انشئ dir && انتقل dir

# Search in file
محيط> ابحث "word" file.txt
```

## Architecture

```
Input → Lexer → Parser → Executor → Output
         ↓        ↓         ↓
      Tokens    AST    CommandResult
```

- **Lexer**: Tokenizes input with full Arabic/Unicode support
- **Parser**: Builds AST for pipelines, redirections, and operators
- **Executor**: Executes commands and handles data flow

## Testing

```bash
cargo test
# 29 tests passing
```

See the Arabic documentation above for complete details.
